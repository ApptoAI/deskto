{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["ELECTRON_RENDERER_URL", "PATH", "DESKTO_FORCE_ONBOARDING", "DESKTO_SIMULATE_NO_AGENTS"],
  "globalPassThroughEnv": ["SHELL"],
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "out/**", ".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
